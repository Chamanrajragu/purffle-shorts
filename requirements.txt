# Core
requests>=2.31
python-dotenv>=1.0
Pillow>=10.1
edge-tts>=7.0
imageio-ffmpeg>=0.5        # bundled ffmpeg if none is installed
tzdata; sys_platform == "win32"

# YouTube upload
google-api-python-client>=2.100
google-auth>=2.23
google-auth-oauthlib>=1.1

# Claude (Anthropic) provider — other LLMs need no extra package
anthropic>=1.0

# Optional extras (install only what you use):
#   faster-whisper   precise word timing for OpenAI/Kokoro/Coqui/system voices
#   kokoro soundfile local open-weight voice (Kokoro-82M)
#   coqui-tts        local Coqui voices
